STATE_CAPTURE_SOCK=/run/state/collect.sock
STATE_CAPTURE_ANNOUNCE_DIR=/var/lib/state-capture/announce
STATE_CAPTURE_SPOOL_DIR=/var/lib/state-capture/spool
# Tick while --serve is running (seconds).
# STATE_CAPTURE_TICK_SECS=60
